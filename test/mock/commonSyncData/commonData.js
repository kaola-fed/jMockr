/**
 * 目前导出顶部导航栏里的链接配置
 * 以后如果有其他多页面公用的数据, 也从这个文件中导出
 * @author hzyubaoquan
 *
 */

const navMenu = require('./navMenu/index');

module.exports.menu = navMenu;
module.exports.userRealName = 'commander';
