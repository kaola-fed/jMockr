{
  "name": "mock_server",
  "version": "1.0.0",
  "description": "for seperate frontend and backend development",
  "main": "app.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "startNode": "nodemon app.js",
    "startSync": "browser-sync start --proxy \"localhost:3000\" --files \"../**/*.css, ../**/*.js, !../mock_server/**/*.*, ../**/*.html, ../**/*.ftl\""
  },
  "author": "hzyubaoquan",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "freemarker.js": "^1.2.2",
    "image-size": "^0.5.0",
    "morgan": "^1.7.0",
    "multer": "^1.2.0",
    "node.extend": "^1.1.6"
  },
  "devDependencies": {
    "browser-sync": "^2.16.0",
    "nodemon": "^1.10.2"
  }
}
